# Copy to .env and set RADICALE_BIND to this host's Tailscale IPv4.
# Find it with: tailscale ip -4

# Tailscale IP of the NAS/home server (NOT 0.0.0.0 unless you know what you are doing)
RADICALE_BIND=100.64.0.10

# Optional image pin
# RADICALE_IMAGE=tomsquest/docker-radicale:latest
