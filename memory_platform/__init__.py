"""memory_platform — Odysseus-native agent memory platform.

Hybrid retrieval (BM25 + dense + RRF), graph memory, drift protection,
sleep-time consolidation, and brain view — all paths under Odysseus DATA_DIR.
"""
