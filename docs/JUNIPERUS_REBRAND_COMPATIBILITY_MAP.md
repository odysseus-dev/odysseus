# JUNIPERUS REBRAND COMPATIBILITY MAP

_This document maps legacy Odysseus identifiers to their Juniperus equivalents.  
Legacy keys are preserved for backward compatibility unless marked "removed"._

---

## Environment Variables

| Legacy | Juniperus Alias | Current Behavior |
|---|---|---|
| `ODYSSEUS_INTERNAL_TOKEN` | `JUNIPERUS_INTERNAL_TOKEN` | Both accepted; JUNIPERUS takes precedence if set. |
| `ODYSSEUS_INPROCESS_TASKS` | *(unchanged)* | Retained as functional key. |
| `ODYSSEUS_DISABLE_MCP` | *(unchanged)* | Retained as functional key. |
| `ODYSSEUS_FALLBACK_OWNER` | *(unchanged)* | Retained as functional key. |
| `ODYSSEUS_SINGLE_USER` | *(unchanged)* | Retained as functional key. |
| `ODYSSEUS_ADMIN_USER` | *(unchanged)* | Retained as functional key. |
| `ODYSSEUS_ADMIN_PASSWORD` | *(unchanged)* | Retained as functional key. |
| `ODYSSEUS_PERSONAL_UPLOAD_MAX_BYTES` | *(unchanged)* | Retained as functional key. |
| `ODYSSEUS_MAIL_ATTACHMENTS_DIR` | *(unchanged)* | Retained as functional key. |
| `ODYSSEUS_PORT` | `JUNIPERUS_PORT` | Both accepted in startup scripts; JUNIPERUS takes precedence. |
| `ODYSSEUS_NO_OPEN` | `JUNIPERUS_NO_OPEN` | Both accepted; JUNIPERUS takes precedence. |

---

## HTTP Headers

| Legacy | Juniperus | Behavior |
|---|---|---|
| `X-Odysseus-Internal-Token` | `X-Juniperus-Internal-Token` | Both accepted; JUNIPERUS header checked first. |
| `X-Juniperus-Internal-Token` | *(primary)* | Preferred; used by current internal tooling. |

---

## LocalStorage Keys

| Legacy | Juniperus | Behavior |
|---|---|---|
| `odysseus-theme` | `juniperus-theme` | Newer key read first; legacy key read on fallback. Migration occurs on theme save. |

---

## Files Retained for Compatibility

| Path | Note |
|---|---|
| `odysseus-ui.service` | Systemd service file (renamed target binary). |
| `start-macos.sh` | Startup script (uses `ODYSSEUS_*` env vars). |
| `scripts/odysseus-*` (symlinks) | Legacy command conveniences (unchanged). |
| `.bak` files (`app.py.juniperus030.bak`, etc.) | Historical backups (ignored in scans). |

---

## Removed / Redirected

| Legacy | New Location |
|---|---|
| `Odysseus Chat` (visible title) | Replaced by `Juniperus - Gnexus Operations Console`. |
| `Odysseus` (app shell title) | Replaced by `Juniperus`. |

---

## Classification Legend

- **Visible user-facing branding**: Must be Juniperus/Gnexus; flag if found.
- **Docs/history**: Allowed; informational only.
- **Functional compatibility key**: Preserved; critical for operation.
- **Internal API/header/localStorage key**: Preserved with Juniperus alias.
- **Test fixture or migration history**: Allowed; used by test suite.

---

*Workspace root: `C:\Users\iamcy\CymaticsDev`*  
*Generated by `scripts/gnexus/Scan-JuniperusBranding.ps1`*
