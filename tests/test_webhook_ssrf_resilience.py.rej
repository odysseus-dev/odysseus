diff a/tests/test_webhook_ssrf_resilience.py b/tests/test_webhook_ssrf_resilience.py	(rejected hunks)
@@ -49,6 +49,14 @@ def test_webhook_url_ssrf_mitigation():
 
 @pytest.mark.asyncio
 async def test_webhook_delivery_uses_naive_utc_timestamps(monkeypatch):
+    # A sibling test (test_review_regressions) re-imports route modules under a
+    # stubbed core.database at RUN time, which can leave a cached
+    # src.webhook_manager whose `from src.database import Webhook` is frozen to a
+    # placeholder (Webhook -> object) — then _deliver's `Webhook.id` raises and
+    # delivery silently "fails". Drop the cached webhook_manager + its
+    # src.database dependency so they re-import against the real core.database.
+    sys.modules.pop("src.webhook_manager", None)
+    sys.modules.pop("src.database", None)
     import src.webhook_manager as wm
 
     class _Query:
