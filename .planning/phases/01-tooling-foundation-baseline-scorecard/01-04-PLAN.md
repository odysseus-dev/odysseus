---
phase: 01-tooling-foundation-baseline-scorecard
plan: 04
type: execute
wave: 4
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - scripts/scorecard.py
  - .planning/scorecard/baseline.json
  - .planning/scorecard/SCORECARD.md
  - .planning/scorecard/security-triage.md
autonomous: true
requirements: [BASE-01]
user_setup: []

must_haves:
  truths:
    - "scripts/scorecard.py --write produces a complete baseline.json with all 7 D-14 metrics and recorded thresholds (SC#2)"
    - "scripts/scorecard.py --check recomputes metrics and exits nonzero on a seeded regression (the ratchet, D-11)"
    - "SCORECARD.md is rendered from baseline.json (JSON is the source of truth, D-10)"
    - "The authenticated-endpoint enumeration list mirrors app.py's inline allow-list exactly (input for Phase 2 COV-03 / Phase 5 SEC-01)"
  artifacts:
    - path: "scripts/scorecard.py"
      provides: "Re-runnable JSON-first metric generator with --write and --check modes"
      contains: "def main"
    - path: ".planning/scorecard/baseline.json"
      provides: "Source-of-truth baseline metrics + thresholds"
      contains: "schema_version"
    - path: ".planning/scorecard/SCORECARD.md"
      provides: "Human-readable rendered view of baseline.json"
    - path: ".planning/scorecard/security-triage.md"
      provides: "Committed triage notes referenced by the scorecard security-findings list"
  key_links:
    - from: "scripts/scorecard.py"
      to: "app.routes + app.py allow-list"
      via: "import app; walk APIRoute; classify against AUTH_EXEMPT_* values"
      pattern: "app\\.routes"
    - from: "scripts/scorecard.py --write"
      to: ".planning/scorecard/baseline.json"
      via: "json.dump of computed metrics"
      pattern: "baseline.json"
---

<objective>
Build the only bespoke code in this phase: `scripts/scorecard.py`, a re-runnable JSON-first generator that measures all seven D-14 metrics by shelling out to the mature tools (ruff, coverage, bandit, pip-audit) and reading the FastAPI route table + import-time perf. `--write` regenerates `baseline.json` + the rendered `SCORECARD.md`; `--check` recomputes and ratchet-compares to the committed baseline, exiting nonzero on regression (D-11). Run `--write` once to capture the clean post-ruff/post-mypy baseline.

Purpose: Implements BASE-01 / SC#2 — an objective, machine-comparable baseline with no-regression thresholds reused at every phase boundary (D-10/D-12). The authenticated-endpoint enumeration it emits is the input list for Phase 2 COV-03 and Phase 5 SEC-01. Behavior-preserving: the script only measures existing code, changes nothing in the app.
Output: `scripts/scorecard.py`, `.planning/scorecard/baseline.json`, `.planning/scorecard/SCORECARD.md`, `.planning/scorecard/security-triage.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-tooling-foundation-baseline-scorecard/01-CONTEXT.md
@.planning/phases/01-tooling-foundation-baseline-scorecard/01-RESEARCH.md
@.planning/phases/01-tooling-foundation-baseline-scorecard/01-PATTERNS.md
@.planning/phases/01-tooling-foundation-baseline-scorecard/01-01-SUMMARY.md
@scripts/claim_ownerless.py
@scripts/index_documents.py
@scripts/hf_download.py
</context>

<artifacts_this_phase_produces>
This plan produces:
- `scripts/scorecard.py` — functions: a per-metric computer for each of the 7 metrics (module_line_counts, typed_pct via AST, ruff_findings, security_findings, coverage, perf, auth_endpoints), a write_baseline (--write), a check_ratchet (--check, nonzero on regression), a render_markdown, and main() with argparse --write/--check flags
- `.planning/scorecard/baseline.json` — source of truth: schema_version, generated_at, git_sha, tool_versions, the 7 metrics, thresholds (mode "ratchet")
- `.planning/scorecard/SCORECARD.md` — rendered human view
- `.planning/scorecard/security-triage.md` — triage notes for the security-findings list (referenced by baseline.json)
- CLI flags: --write, --check
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Build scripts/scorecard.py — the 7-metric generator + write/check modes</name>
  <files>scripts/scorecard.py</files>
  <read_first>
    - scripts/claim_ownerless.py (script skeleton: shebang, module docstring with Usage, sys.path insert, main() + __main__, lazy in-function imports)
    - scripts/index_documents.py (logging.basicConfig + module logger + f-string logs)
    - scripts/hf_download.py (argparse pattern for --write/--check)
    - 01-PATTERNS.md section scripts/scorecard.py lines 251-341 (analogs, skeleton, route-table+allow-list read, shell-out-don't-reimplement)
    - 01-RESEARCH.md lines 508-560 (per-metric measurement table, JSON schema shape, ratchet enforcement) and lines 521-523 (auth classification via the middleware allow-list, NOT Depends-scanning)
    - app.py:161-194 (the inline AUTH_EXEMPT_EXACT / AUTH_EXEMPT_PREFIXES / AUTH_EXEMPT_PATTERNS / _is_auth_exempt — note these only exist when AUTH_ENABLED is true, so the script must encode the allow-list VALUES verbatim, not import the symbols) and app.py:770-783 (/api/version, /api/health for the perf key-path sample)
  </read_first>
  <action>
    Create scripts/scorecard.py following the house script skeleton: a shebang, a module docstring with the two-mode Usage block, sys.path.insert(0, repo-root), logging.basicConfig + module logger (f-string logs, double-quoted strings), argparse with --write and --check (D-12), main() + the __main__ guard.
    Implement seven metric computers, each shelling out to the mature tool (do NOT reimplement them — RESEARCH lines 235, 513-521):
    (1) module_line_counts — pathlib walk of src/, routes/, core/, services/, mcp_servers/, companion/, app.py; physical lines per .py; exclude tests/, static/, vendored; also emit max_module_loc.
    (2) typed_pct — AST-walk (ast.FunctionDef / ast.AsyncFunctionDef): "typed" = has return annotation AND all non-self/cls args annotated (tool-version-independent per Open Q3); emit overall + by_module.
    (3) ruff_findings — ruff check . --output-format=json parsed to total + by_code (expect 0 at baseline).
    (4) security_findings — merge bandit -c pyproject.toml -r <dirs> -f json (post-suppression) + pip-audit -r requirements.lock -f json, each with severity + a triaged flag referencing security-triage.md.
    (5) coverage — pytest --cov=. --cov-report=json then read coverage.json files{} per-file percent + overall.
    (6) perf — time import app cold, averaged over N runs (record N, python, machine metadata); optionally one TestClient GET to /api/health (app.py:775, confirmed present) for a key-path latency sample; fall back to import-time-only if TestClient errors (perf is a guardrail, not a gate — A2).
    (7) auth_endpoints — import app, walk app.app.routes for APIRoutes; classify each via a LOCAL copy of the allow-list values from app.py:162-187 (the AUTH_EXEMPT_EXACT set, the /static prefix, the ^/api/tasks/[^/]+/webhook/[^/]+/?$ pattern) and a local _is_auth_exempt mirroring app.py:189-194 — authenticated = NOT exempt. Emit {path, methods (sorted), authenticated} per route. Add a comment that this list is the input for Phase 2 COV-03 / Phase 5 SEC-01 and MUST be kept in sync with app.py's allow-list.
    Implement the --write path: assemble the JSON document (schema_version 1, generated_at, git_sha from git rev-parse HEAD, tool_versions, the 7 metrics, and a thresholds block with mode "ratchet" per D-11 — ruff_findings_total max=0, typed_pct_overall min=baseline, max_module_loc max=baseline, coverage_overall_pct min=baseline, security_high_critical max=0); write .planning/scorecard/baseline.json then render .planning/scorecard/SCORECARD.md from it (D-10: JSON is source of truth, markdown is a view).
    Implement the --check path: recompute metrics, compare to the committed baseline.json thresholds, log each comparison, exit nonzero if any metric regressed (ruff findings rise above max, typed_pct drops below min, max_module_loc grows, coverage drops, high/critical security findings appear). Absolute targets (ruff=0, security high/critical=0) are encoded directly; everything else is relative-to-baseline.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('scripts/scorecard.py').read()); print('PARSE_OK')"; grep -q 'app.routes' scripts/scorecard.py &amp;&amp; grep -qE 'AUTH_EXEMPT_EXACT|_is_auth_exempt' scripts/scorecard.py &amp;&amp; grep -qE 'output-format=json|output_format' scripts/scorecard.py</automated>
  </verify>
  <acceptance_criteria>
    scripts/scorecard.py parses; defines main() with argparse --write/--check; computes all 7 metrics by shelling out to ruff/coverage/bandit/pip-audit and reading app.routes; encodes the auth allow-list VALUES locally (mirroring app.py:162-194, not importing the AUTH_ENABLED-gated symbols); uses house conventions (module docstring, f-string logging, sys.path insert). It does not modify any application code.
  </acceptance_criteria>
  <done>The bespoke generator exists with both modes, shelling out to mature tools and reading the route table.</done>
</task>

<task type="auto">
  <name>Task 2: Run write to capture the clean baseline; verify the check ratchet on a seeded regression</name>
  <files>.planning/scorecard/baseline.json, .planning/scorecard/SCORECARD.md, .planning/scorecard/security-triage.md</files>
  <read_first>
    - 01-RESEARCH.md lines 526-560 (JSON schema shape + ratchet enforcement semantics)
    - 01-VALIDATION.md Per-Task Verification Map row "scorecard generator + baseline.json" (the write/check acceptance)
    - 01-CONTEXT.md D-10, D-11, D-13
  </read_first>
  <action>
    Ensure the dev tools and core lock are installed (ruff==0.15.15, mypy==2.1.0, bandit==1.9.4, pip-audit==2.10.0, pytest-cov, and -r requirements.lock). Create .planning/scorecard/ and a security-triage.md stub (sections for bandit findings and pip-audit CVEs, each with a triage-rationale column — populated as findings arise; referenced by baseline.json's security_findings triaged flags). Run python scripts/scorecard.py --write to produce baseline.json (all 7 metrics populated; ruff_findings.total == 0 reflecting Plan 02's clean baseline; typed_pct reflecting the post-mypy state) and the rendered SCORECARD.md.
    Verify the ratchet works (D-11): run python scripts/scorecard.py --check against the just-written baseline (exit 0). Then seed a transient real regression (e.g. a temporary unused import in a source file to push ruff_findings above 0 — do NOT edit baseline.json thresholds; regress an actual measured metric) and confirm --check exits nonzero; revert the seeded change so the tree is clean. Record both outcomes in the SUMMARY. This is the SC#2 ratchet acceptance.
  </action>
  <verify>
    <automated>python scripts/scorecard.py --write &amp;&amp; python -c "import json; d=json.load(open('.planning/scorecard/baseline.json')); m=d['metrics']; assert all(k in m for k in ['module_line_counts','typed_pct','ruff_findings','security_findings','coverage','perf','auth_endpoints']); assert 'thresholds' in d; print('SCORECARD_OK')" &amp;&amp; test -f .planning/scorecard/SCORECARD.md &amp;&amp; python scripts/scorecard.py --check</automated>
  </verify>
  <acceptance_criteria>
    baseline.json contains all 7 metrics + a thresholds block; SCORECARD.md exists and is rendered from it; scorecard.py --check exits 0 against the clean baseline and nonzero against a seeded real regression (recorded in SUMMARY); the working tree is clean after the seeded check is reverted.
  </acceptance_criteria>
  <done>The clean baseline is captured as the source of truth and the ratchet is demonstrated to catch regression (SC#2).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app import (scorecard) → app module-level code | Importing app to walk routes/perf executes app's top-level init |
| scorecard security_findings → security-triage.md | Triage rationale governs which findings are treated as accepted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | auth_endpoints list drifting out of sync with app.py's allow-list | mitigate | Encode the allow-list VALUES with an explicit "keep in sync with app.py:162-194" comment; the list is the basis for Phase 2/5 isolation tests, so divergence is a security gap |
| T-04-02 | Information disclosure | scorecard importing app may execute side-effectful startup | accept | Import-only (no serving); perf metric times exactly this; falls back to import-only timing if TestClient errors (A2) |
| T-04-03 | Repudiation | security findings silently dropped from the baseline | mitigate | Every bandit/pip-audit finding recorded with severity + triaged flag referencing the committed security-triage.md (V14 config-as-code) |
| T-04-SC | Tampering | pip-audit run against an unhashed/altered lock | mitigate | pip-audit reads requirements.lock (hashed, from Plan 01); high/critical findings are an absolute threshold (max=0) |
</threat_model>

<verification>
- `python scripts/scorecard.py --write` produces baseline.json with all 7 metrics + thresholds, and SCORECARD.md.
- `python scripts/scorecard.py --check` exits 0 on the clean baseline; nonzero on a seeded real regression.
- auth_endpoints enumeration mirrors app.py:162-194's allow-list values.
- baseline.json lives in `.planning/scorecard/` (D-13); JSON is source of truth, markdown is rendered (D-10).
- The script modifies no application code.
</verification>

<success_criteria>
- BASE-01 / SC#2: a baseline scorecard exists in `.planning/scorecard/` with measured values for all 7 metrics and recorded ratchet thresholds; the generator is re-runnable and the `--check` ratchet catches regressions.
</success_criteria>

<output>
Create `.planning/phases/01-tooling-foundation-baseline-scorecard/01-04-SUMMARY.md` when done. Record: the measured baseline values (max_module_loc, typed_pct_overall, coverage_overall_pct, ruff_findings.total, auth_endpoints count), the seeded-regression outcome, any perf TestClient fallback.
</output>
