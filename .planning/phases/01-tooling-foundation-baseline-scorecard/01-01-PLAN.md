---
phase: 01-tooling-foundation-baseline-scorecard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.in
  - requirements-optional.in
  - requirements-dev.in
  - requirements.lock
  - requirements-optional.lock
  - requirements-dev.lock
autonomous: true
requirements: [TOOL-03]
user_setup: []

must_haves:
  truths:
    - "A hash-pinned core lockfile installs clean on a second clean python:3.12-slim container with no ResolutionImpossible (SC#3)"
    - "Dev tool versions (ruff, mypy, bandit, pip-audit, pytest-cov) are pinned and resolvable, separate from the runtime lock"
    - "pytest-cov is installable and the existing suite is stable under --cov instrumentation (Pitfall 11)"
  artifacts:
    - path: "requirements.in"
      provides: "Direct core runtime deps (intent), copied from requirements.txt minus pytest/pytest-asyncio"
      contains: "chromadb-client"
    - path: "requirements-optional.in"
      provides: "AGPL/optional dep intent (PyMuPDF, markitdown), preserving the quarantine boundary"
      contains: "PyMuPDF"
    - path: "requirements-dev.in"
      provides: "Pinned dev/CI tool versions"
      contains: "ruff==0.15.15"
    - path: "requirements.lock"
      provides: "uv-compiled, --generate-hashes core lock"
      contains: "--hash=sha256:"
    - path: "requirements-optional.lock"
      provides: "uv-compiled optional/AGPL lock"
      contains: "--hash=sha256:"
    - path: "requirements-dev.lock"
      provides: "uv-compiled dev-tool lock"
      contains: "ruff=="
  key_links:
    - from: "requirements.in"
      to: "requirements.lock"
      via: "uv pip compile --generate-hashes inside python:3.12-slim"
      pattern: "uv pip compile"
---

<objective>
Establish the dependency-locking foundation and Wave-0 tooling prerequisites: author the `.in` intent files (core, optional, dev), compile fully-pinned hashed lockfiles inside Docker `python:3.12-slim`, validate the core lock installs clean on a second clean container, and confirm the existing pytest suite is stable under `--cov` instrumentation.

Purpose: The lockfile is the supply-chain root of trust (per D-04/D-09 — CI installs from it with `--require-hashes`) and the dev-tool pins (per Wave 0 / RESEARCH "Standard Stack") are referenced by every later CI gate. This plan ships them before anything consumes them. No application code changes (behavior-preserving).
Output: `requirements.in`, `requirements-optional.in`, `requirements-dev.in`, and their three compiled `.lock` files; a confirmed-stable coverage baseline run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-tooling-foundation-baseline-scorecard/01-CONTEXT.md
@.planning/phases/01-tooling-foundation-baseline-scorecard/01-RESEARCH.md
@.planning/phases/01-tooling-foundation-baseline-scorecard/01-PATTERNS.md
@requirements.txt
@requirements-optional.txt
</context>

<artifacts_this_phase_produces>
This plan produces (consumed by later Phase-1 plans):
- `requirements.in` — direct core deps (no pins, with why-comments)
- `requirements-optional.in` — PyMuPDF (AGPL) + markitdown, with the verbatim AGPL note
- `requirements-dev.in` — `ruff==0.15.15`, `mypy==2.1.0`, `bandit==1.9.4`, `pip-audit==2.10.0`, `pytest`, `pytest-asyncio`, `pytest-cov`
- `requirements.lock` — hashed core lock (consumed by Plan 03 mypy job, Plan 04 pip-audit metric, Plan 05 CI install-from-lock jobs)
- `requirements-optional.lock` — hashed optional lock
- `requirements-dev.lock` — hashed dev-tool lock
- (conditional) `requirements-ml.in` / `requirements-ml.lock` — ONLY if the unified core resolve fails (D-06 fallback)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author the three .in intent files</name>
  <files>requirements.in, requirements-optional.in, requirements-dev.in</files>
  <read_first>
    - requirements.txt (the direct-dep source set — copy verbatim minus the test deps)
    - requirements-optional.txt (optional/AGPL set + the load-bearing AGPL comment)
    - 01-PATTERNS.md §"requirements.in" / §"requirements-optional.in" / §"requirements-dev.in" (exact dep lists + comment conventions to replicate)
    - 01-RESEARCH.md lines 386-387 (which deps move where) and lines 116-125 (dev-tool pinned set)
  </read_first>
  <action>
    Create `requirements.in` with the 25 direct core deps from `requirements.txt` lines 1-39 (D-07): keep the existing why-comment blocks verbatim (the chromadb-client/fastembed RAG block, the markdown/icalendar/dateutil/caldav rationale). Drop `pytest` and `pytest-asyncio` (requirements.txt:40-41) — they move to dev. Add a one-line header comment marking this as direct-deps intent compiled to `requirements.lock`.
    Create `requirements-optional.in` with `faster-whisper`, `duckduckgo-search`, `PyMuPDF`, and `markitdown[docx,pptx,xlsx,xls]==0.1.5` (per D-08), preserving the verbatim AGPL-3.0 note from `requirements-optional.txt:22-24` (load-bearing per ACKNOWLEDGMENTS.md) and the markitdown rationale block.
    Create `requirements-dev.in` (Wave 0) with the pinned tool set: `ruff==0.15.15`, `mypy==2.1.0`, `bandit==1.9.4`, `pip-audit==2.10.0`, `pytest`, `pytest-asyncio`, `pytest-cov` (Pitfall 9 — pin so a tool bump cannot silently break CI). Add a header comment that these are dev/CI tools and MUST stay out of `requirements.lock` (behavior-preserving: runtime install surface unchanged).
    Use double-quoted-free flat-list style matching `requirements.txt`; f-string/log conventions do not apply (these are text config files). No fenced code in the file beyond the dependency lines + comments.
  </action>
  <verify>
    <automated>test -f requirements.in &amp;&amp; test -f requirements-optional.in &amp;&amp; test -f requirements-dev.in &amp;&amp; grep -q 'chromadb-client' requirements.in &amp;&amp; grep -q 'PyMuPDF' requirements-optional.in &amp;&amp; grep -q 'ruff==0.15.15' requirements-dev.in &amp;&amp; ! grep -qE '^pytest' requirements.in</automated>
  </verify>
  <acceptance_criteria>
    `requirements.in` contains all 25 core direct deps and NO `pytest`/`pytest-asyncio` lines; `requirements-optional.in` contains `PyMuPDF` + `markitdown[...]==0.1.5` and the verbatim AGPL note; `requirements-dev.in` contains all five pinned tools + pytest trio. All three commands above exit 0.
  </acceptance_criteria>
  <done>Three `.in` files exist with the correct dep partitioning and preserved comments.</done>
</task>

<task type="auto">
  <name>Task 2: Compile hashed lockfiles inside python:3.12-slim (with D-06 ML-fallback branch)</name>
  <files>requirements.lock, requirements-optional.lock, requirements-dev.lock</files>
  <read_first>
    - 01-RESEARCH.md lines 360-404 (the exact Docker compile recipe, --generate-hashes/--python-version flags, ML-group conflict detection + recovery)
    - 01-CONTEXT.md D-06, D-09 (single unified core lock; compile inside Docker; fallback only if unified resolve fails)
    - 01-PATTERNS.md §"requirements.lock ... (NO ANALOG)" (generation pattern, fallback)
  </read_first>
  <action>
    Confirm Docker can pull `python:3.12-slim` first (no fallback exists — required for D-09 + SC#3). Run the locked recipe: `docker run --rm -v "$PWD":/w -w /w python:3.12-slim bash -c 'set -euo pipefail; pip install --no-cache-dir uv==0.11.18; uv pip compile requirements.in --generate-hashes --python-version 3.12 --output-file requirements.lock; uv pip compile requirements-optional.in --generate-hashes --python-version 3.12 --output-file requirements-optional.lock; uv pip compile requirements-dev.in --generate-hashes --python-version 3.12 --output-file requirements-dev.lock'`. Do NOT use `--universal` (D-09 locks the single-platform linux/3.12 lock for CI parity). Do NOT generate on the local Python 3.14 host (drift risk).
    Capture stderr. D-06 ML-fallback branch: if the core `uv pip compile requirements.in` fails with `ResolutionImpossible` / `No solution found` naming chromadb-client/fastembed/onnxruntime/numpy/protobuf — first try pinning the shared transitive(s) explicitly in `requirements.in` and recompile; if still impossible, split a new `requirements-ml.in` (chromadb-client + fastembed) → `requirements-ml.lock`, keep the core lock without them, add `files_modified` note, and document the split rationale in a comment at the top of `requirements-ml.in`. Record the actual outcome (unified vs split) in the SUMMARY.
    Verify the three lock files contain `--hash=sha256:` lines (proves `--generate-hashes` took effect).
  </action>
  <verify>
    <automated>test -f requirements.lock &amp;&amp; test -f requirements-dev.lock &amp;&amp; grep -q -- '--hash=sha256:' requirements.lock &amp;&amp; grep -q -- '--hash=sha256:' requirements-dev.lock</automated>
  </verify>
  <acceptance_criteria>
    `requirements.lock`, `requirements-optional.lock`, `requirements-dev.lock` exist and each contains `--hash=sha256:` lines. If the ML fallback triggered, `requirements-ml.lock` exists with a documented split-rationale comment and the SUMMARY records why. Generation happened inside `python:3.12-slim` (recorded in SUMMARY), not the local host.
  </acceptance_criteria>
  <done>Three (or four, with ML split) hashed lockfiles are generated reproducibly inside the CI base image.</done>
</task>

<task type="auto">
  <name>Task 3: Validate lock on a second clean container (SC#3) + confirm coverage stability (Pitfall 11)</name>
  <files>requirements.lock</files>
  <read_first>
    - 01-RESEARCH.md lines 396-404 (SC#3 second-container validation command) and lines 626, 634-637 (Pitfall 11 coverage-stability requirement)
    - 01-VALIDATION.md §"Wave 0 Requirements" and §"Manual-Only Verifications" (the SC#3 second-env check)
  </read_first>
  <action>
    Run the SC#3 reproducibility proof in a fresh container distinct from the generation step: `docker run --rm -v "$PWD":/w -w /w python:3.12-slim bash -c 'pip install --require-hashes --no-cache-dir -r requirements.lock'` and require exit 0 (no `ResolutionImpossible`). This is the locked SC#3 acceptance demonstration.
    Then run the one-time coverage-stability check (Pitfall 11) — install the dev lock and run the suite under coverage in a container: `docker run --rm -v "$PWD":/w -w /w python:3.12-slim bash -c 'pip install --require-hashes --no-cache-dir -r requirements.lock && pip install --no-cache-dir pytest pytest-asyncio pytest-cov && pytest --cov=. -q'`. The suite must run green and deterministically. If async coverage flakiness appears, note that `[tool.coverage.run] concurrency = ["greenlet", "thread"]` will be needed in Plan 04's pyproject config and record it in the SUMMARY (do not add it here — pyproject is owned by Plans 02-04).
    Record the measured full-suite runtime in the SUMMARY (feeds the VALIDATION feedback-latency budget and the Plan 04 perf metric metadata).
  </action>
  <verify>
    <automated>docker run --rm -v "$PWD":/w -w /w python:3.12-slim bash -c 'pip install --require-hashes --no-cache-dir -r requirements.lock' &amp;&amp; echo SC3_PASS</automated>
  </verify>
  <acceptance_criteria>
    The second-container `pip install --require-hashes -r requirements.lock` exits 0 (prints `SC3_PASS`). The `pytest --cov=.` run completes green; any async-coverage flakiness is recorded as a Plan 04 note. Full-suite runtime is recorded in the SUMMARY.
  </acceptance_criteria>
  <done>SC#3 is demonstrated; coverage instrumentation is confirmed stable before any later plan trusts the coverage metric.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI/index → repo lockfile | Third-party package content crosses into the build at compile time |
| Lockfile → CI/runtime install | Resolved deps are installed into the running environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | `requirements.lock` (dependency content) | mitigate | `uv pip compile --generate-hashes`; second-container install uses `--require-hashes` so altered artifacts fail (ASVS V10) |
| T-01-02 | Tampering | unpinned drift across local 3.14 vs CI 3.12 | mitigate | Compile inside `python:3.12-slim` only; single-platform lock for exact CI parity (D-09) |
| T-01-SC | Tampering | new transitive package entering the lock (slopsquat risk) | mitigate | Review the compiled lock diff before commit; pip-audit gate added in Plan 04/05; dev tools kept out of the runtime lock. All Phase-1 packages are mainstream, high-trust, version-verified against PyPI this session (RESEARCH Package Legitimacy Audit — no [SLOP]/[SUS]); no new-package human-verify checkpoint required |
</threat_model>

<verification>
- `requirements.lock` / `-optional.lock` / `-dev.lock` exist with `--hash=sha256:` lines.
- SC#3: `docker run ... python:3.12-slim pip install --require-hashes -r requirements.lock` → exit 0.
- `pytest --cov=.` runs green and deterministic (Pitfall 11).
- Dev tools (ruff/mypy/bandit/pip-audit/pytest-cov) appear in `requirements-dev.lock`, NOT in `requirements.lock`.
</verification>

<success_criteria>
- TOOL-03 / SC#3 satisfied: a committed lockfile generated via `uv pip compile` on `python:3.12-slim` installs clean on a second clean Linux container without `ResolutionImpossible`.
- Wave 0 prerequisites met: dev-tool pins + pytest-cov available; coverage suite confirmed stable.
</success_criteria>

<output>
Create `.planning/phases/01-tooling-foundation-baseline-scorecard/01-01-SUMMARY.md` when done. Record: unified-vs-ML-split outcome, full-suite runtime, any coverage-concurrency note for Plan 04.
</output>
