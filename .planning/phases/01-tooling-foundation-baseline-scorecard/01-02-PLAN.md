---
phase: 01-tooling-foundation-baseline-scorecard
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - pyproject.toml
  - .git-blame-ignore-revs
autonomous: true
requirements: [TOOL-01]
user_setup: []

must_haves:
  truths:
    - "ruff check . reports zero findings on the clean baseline (SC#5)"
    - "ruff format --check . exits 0 — formatter output is stable (SC#1)"
    - "The full pytest suite stays green after the --fix commit and after the format commit (behavior preserved)"
    - "ruff is pinned to an explicit version; config uses no select=['ALL'] and no preview=true (SC#5)"
  artifacts:
    - path: "pyproject.toml"
      provides: "[tool.ruff], [tool.ruff.lint], [tool.ruff.format] config tables appended below the existing pytest block"
      contains: "[tool.ruff]"
    - path: ".git-blame-ignore-revs"
      provides: "Records the bulk ruff-format commit SHA so git blame skips the mechanical reformat"
  key_links:
    - from: "pyproject.toml [tool.ruff.lint]"
      to: "ruff check ."
      via: "select = ['E','F','I','W','UP','B']; ignore = ['E501']"
      pattern: "select = \\[.*\"E\""
---

<objective>
Adopt ruff lint + format and bring the repo to a genuine zero-finding baseline through the locked three-commit atomic sequence (D-18): (1) add `[tool.ruff]` config with no code change, (2) `ruff check --fix` auto-fixes, (3) whole-repo `ruff format` recorded in `.git-blame-ignore-revs` — then resolve any residual findings to zero via config-level `per-file-ignores`. The full pytest suite is the behavior contract and must stay green after every code-touching commit.

Purpose: Implements TOOL-01 and ROADMAP SC#5 (pinned ruff, explicit rule set, no ALL/preview). Sequencing is the core phase risk — fixing before formatting, keeping the format commit purely mechanical, and gating each code-touching step on full pytest is the locked mitigation (RESEARCH §Sequencing).
Output: Extended `pyproject.toml` with ruff config; `.git-blame-ignore-revs`; a zero-finding, stably-formatted repo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-tooling-foundation-baseline-scorecard/01-CONTEXT.md
@.planning/phases/01-tooling-foundation-baseline-scorecard/01-RESEARCH.md
@.planning/phases/01-tooling-foundation-baseline-scorecard/01-PATTERNS.md
@pyproject.toml
</context>

<artifacts_this_phase_produces>
This plan produces:
- `pyproject.toml` (EXTEND) — `[tool.ruff]` (`target-version="py312"`, `line-length=88`, `extend-exclude=["static/","data/","logs/"]`), `[tool.ruff.lint]` (`select=["E","F","I","W","UP","B"]`, `ignore=["E501"]`, optional `[tool.ruff.lint.per-file-ignores]`), `[tool.ruff.format]` (`quote-style="double"`)
- `.git-blame-ignore-revs` — one line: the SHA of the bulk `ruff format` commit
- (commits) D-18.1 config-only, D-18.2 `--fix`, D-18.3 `format` — three separate atomic commits; format commit purely mechanical
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add [tool.ruff] config (D-18 commit 1 — no code change)</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (the existing 3-line [tool.pytest.ini_options] block — APPEND below it, do not rewrite)
    - 01-PATTERNS.md §"pyproject.toml (EXTEND)" lines 154-188 (exact ruff config shape to append)
    - 01-RESEARCH.md lines 242-301 (ruff config rationale, E501-off interplay, CI invocation)
    - 01-CONTEXT.md D-15, D-16, D-18, D-19
  </read_first>
  <action>
    Append `[tool.ruff]`, `[tool.ruff.lint]`, and `[tool.ruff.format]` tables below the existing `[tool.pytest.ini_options]` block — preserve those 3 lines verbatim (single-config-file convention; never create `ruff.toml`). Set `target-version = "py312"` (per D-19, the shipped runtime), `line-length = 88` (governs formatter wrap only — lint E501 is disabled so length never fails CI), `extend-exclude = ["static/", "data/", "logs/"]`. In `[tool.ruff.lint]` set `select = ["E", "F", "I", "W", "UP", "B"]` (LOCKED families per D-15 — no `select=["ALL"]`, no `preview=true` per SC#5) and `ignore = ["E501"]` (no hard-column convention per D-19/CONVENTIONS.md). In `[tool.ruff.format]` set `quote-style = "double"` (house style). Leave `[tool.ruff.lint.per-file-ignores]` absent for now — add it in Task 4 only if measured residuals warrant it. Commit this as a config-only change (D-18.1); no code is touched, so `ruff check .` will still report findings.
  </action>
  <verify>
    <automated>grep -q '\[tool.ruff\]' pyproject.toml &amp;&amp; grep -q '"UP"' pyproject.toml &amp;&amp; grep -q '"E501"' pyproject.toml &amp;&amp; ! grep -q 'select = \["ALL"\]' pyproject.toml &amp;&amp; ! grep -q 'preview = true' pyproject.toml &amp;&amp; grep -q 'asyncio_mode = "auto"' pyproject.toml</automated>
  </verify>
  <acceptance_criteria>
    `pyproject.toml` contains the three ruff tables with `select=["E","F","I","W","UP","B"]`, `ignore=["E501"]`, `quote-style="double"`, `target-version="py312"`; contains NO `select=["ALL"]` and NO `preview=true`; the original `[tool.pytest.ini_options]` block is intact. This is committed separately as the config-only commit.
  </acceptance_criteria>
  <done>ruff config added in an isolated, revertable config-only commit (D-18.1).</done>
</task>

<task type="auto">
  <name>Task 2: ruff check --fix (D-18 commit 2 — code-touching, gated on full pytest)</name>
  <files>pyproject.toml</files>
  <read_first>
    - 01-RESEARCH.md lines 272-287 (zero-baseline strategy: measure first, safe fixes, then reviewed --unsafe-fixes) and lines 583-601 (the --unsafe-fixes semantic-change caution; keep separate from format)
    - 01-VALIDATION.md §"Sampling Rate" (FULL pytest after every code-touching commit)
    - 01-CONTEXT.md D-18
  </read_first>
  <action>
    Measure first: `ruff check . --output-format=json > /tmp/ruff-before.json` and `ruff check . --statistics` (capture counts per rule for the SUMMARY). Apply safe auto-fixes: `ruff check . --fix` (applies I import-sort, UP rewrites, safe F/B). Run the FULL `pytest` suite immediately — it must stay green (behavior contract). Then OPTIONALLY apply `ruff check . --fix --unsafe-fixes` as a SEPARATE reviewed sub-step: review the diff, run the full suite, and drop any individual unsafe fix that breaks a test (some UP/B rewrites can change semantics). Do NOT bundle unsafe fixes into the later format commit. Commit the result as the `--fix` commit (D-18.2). Record the before/after finding counts and whether unsafe fixes were kept in the SUMMARY.
    Note: this task's `files_modified` lists `pyproject.toml` for ownership, but `--fix` edits many `.py` source files mechanically; those edits are in-scope and verified solely by the full suite staying green (no behavior change permitted).
  </action>
  <verify>
    <automated>uv pip install --system ruff==0.15.15 >/dev/null 2>&amp;1 || pip install ruff==0.15.15 >/dev/null 2>&amp;1; ruff check . --statistics; pytest -q</automated>
  </verify>
  <acceptance_criteria>
    Ruff finding count drops materially versus the measured baseline; the full `pytest` suite is green after `--fix` (and after any kept `--unsafe-fixes`). Unsafe fixes, if applied, were reviewed and are in a clearly-attributable sub-commit, NOT bundled with formatting. Before/after counts recorded.
  </acceptance_criteria>
  <done>Safe (and reviewed-unsafe) auto-fixes applied as an isolated commit; suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Whole-repo ruff format + record blame-ignore SHA (D-18 commit 3)</name>
  <files>.git-blame-ignore-revs, pyproject.toml</files>
  <read_first>
    - 01-PATTERNS.md §".git-blame-ignore-revs (NO ANALOG)" lines 200-209 (the one-SHA-per-line pattern; keep the commit purely mechanical)
    - 01-RESEARCH.md lines 288-295, 601 (format commit recorded in blame-ignore; do NOT bundle unsafe fixes)
    - 01-CONTEXT.md D-16, D-18
  </read_first>
  <action>
    Run `ruff format .` across the whole repo as ONE standalone commit (D-16/D-18.3) — purely mechanical whitespace/layout, nothing else (so blame attribution stays accurate per RESEARCH line 601). Run the FULL `pytest` suite — it must stay green. Commit the formatting. Then create/append `.git-blame-ignore-revs` with the format commit's SHA via `git rev-parse HEAD >> .git-blame-ignore-revs` and commit that one-line file. Add a header comment to `.git-blame-ignore-revs` explaining its purpose (records the bulk ruff-format reformat so `git blame` skips it; GitHub honors this file automatically). Confirm `ruff format --check .` exits 0 (formatter output is stable — SC#1).
  </action>
  <verify>
    <automated>ruff format --check . &amp;&amp; test -f .git-blame-ignore-revs &amp;&amp; pytest -q</automated>
  </verify>
  <acceptance_criteria>
    `ruff format --check .` exits 0; `.git-blame-ignore-revs` exists and contains the format commit SHA; the full `pytest` suite is green. The format commit contains only formatting changes (no unsafe fixes, no config changes).
  </acceptance_criteria>
  <done>Repo formatted in one mechanical commit; SHA recorded; format stable; suite green.</done>
</task>

<task type="auto">
  <name>Task 4: Drive residual ruff findings to zero</name>
  <files>pyproject.toml</files>
  <read_first>
    - 01-RESEARCH.md lines 262-266, 284-287 (residual handling: prefer config-level per-file-ignores over scattered noqa; reserve commented `# noqa: CODE` for one-offs; never bare noqa)
    - 01-PATTERNS.md §"pyproject.toml" (the per-file-ignores example for tests)
    - 01-CONTEXT.md D-15 (no rule-family expansion here)
  </read_first>
  <action>
    Re-run `ruff check . --output-format=json` to enumerate any findings the auto-fix could not resolve. For each residual, prefer a config-level `[tool.ruff.lint.per-file-ignores]` entry in `pyproject.toml` (versioned, greppable, no code churn) — e.g. `"tests/**" = ["F401", "F811"]` if tests import private helpers, `"conftest.py" = ["F401"]`. Use inline `# noqa: CODE  # reason` ONLY for genuinely one-off intentional cases, never bare `# noqa`. Do NOT broaden or narrow the locked rule families (D-15) to reach zero. Drive `ruff check .` to exit 0 with zero findings. Record the final per-file-ignores set and rationale in the SUMMARY.
  </action>
  <verify>
    <automated>ruff check . &amp;&amp; ruff check . --select F401,F811</automated>
  </verify>
  <acceptance_criteria>
    `ruff check .` exits 0 (zero findings); `ruff check . --select F401,F811` exits 0 (also auto-satisfies BASE-02 SC#4 for revert remnants). Any suppressions are config-level per-file-ignores or commented one-off noqa — no bare noqa, no rule-family change.
  </acceptance_criteria>
  <done>ruff is at a true zero-finding baseline; the lint and format gates are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ruff `--fix`/`format` → 560 source files | Automated rewrites cross into production code; behavior must be preserved |
| pyproject config → CI lint/format gates | Config governs what CI hard-blocks on |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `ruff --unsafe-fixes` altering runtime semantics | mitigate | Apply in a separate reviewed sub-commit; full pytest suite (355 files) gates each code-touching step; drop any fix that breaks a test (RESEARCH A4) |
| T-02-02 | Repudiation | `git blame` attribution lost to the bulk reformat | mitigate | Record the format commit SHA in `.git-blame-ignore-revs`; keep that commit purely mechanical (D-16) |
| T-02-03 | Elevation of Privilege | ruff config drift (`select=["ALL"]`/`preview`) breaking CI on tool bumps | accept→mitigate | Locked rule families only; ruff pinned in `requirements-dev.lock` (Plan 01); SC#5 forbids ALL/preview |
| T-02-SC | Tampering | dev tool (ruff) version mismatch local vs CI | mitigate | `ruff==0.15.15` pinned in the dev lock (Plan 01); CI installs that exact version (Plan 05) |
</threat_model>

<verification>
- `ruff check .` exits 0 (zero findings).
- `ruff format --check .` exits 0 (stable format).
- `ruff check . --select F401,F811` exits 0.
- Full `pytest` suite green after the `--fix` commit and after the `format` commit.
- `pyproject.toml`: ruff tables present, no `select=["ALL"]`, no `preview=true`; pytest block intact.
- `.git-blame-ignore-revs` records the format commit SHA.
- Three atomic commits exist in order: config-only → `--fix` → format.
</verification>

<success_criteria>
- TOOL-01 / SC#5: ruff lint + format adopted with config in `pyproject.toml`; clean zero-finding baseline reached via standalone auto-fix/format commits; ruff pinned; explicit stable rule set with no ALL/preview.
- TOOL-01 / SC#1: `ruff format --check` is stable (enforceable in CI).
</success_criteria>

<output>
Create `.planning/phases/01-tooling-foundation-baseline-scorecard/01-02-SUMMARY.md` when done. Record: before/after ruff finding counts, whether `--unsafe-fixes` were kept, the format commit SHA, the final per-file-ignores set.
</output>
