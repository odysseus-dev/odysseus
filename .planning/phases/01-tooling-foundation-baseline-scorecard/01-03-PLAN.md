---
phase: 01-tooling-foundation-baseline-scorecard
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - pyproject.toml
autonomous: true
requirements: [TOOL-02]
user_setup: []

must_haves:
  truths:
    - "mypy --no-incremental --config-file pyproject.toml . exits 0 (zero errors day 1) (SC#5)"
    - "The global mypy config is lenient (no disallow_untyped_defs, no global strict); untyped third-party modules are ignored via explicit overrides"
    - "Per-module [[tool.mypy.overrides]] exist for the known untyped third parties (mcp.*, chromadb.*, etc.)"
  artifacts:
    - path: "pyproject.toml"
      provides: "[tool.mypy] lenient global + [[tool.mypy.overrides]] for untyped third parties; [tool.coverage.run] and [tool.bandit] config tables"
      contains: "[tool.mypy]"
  key_links:
    - from: "pyproject.toml [tool.mypy]"
      to: "mypy --no-incremental ."
      via: "lenient global flags + ignore_missing_imports overrides"
      pattern: "\\[\\[tool.mypy.overrides\\]\\]"
---

<objective>
Adopt mypy with inverted (per-module) strictness: a lenient global baseline that passes with zero errors on day 1 (D-17), explicit `[[tool.mypy.overrides]]` for known-untyped third parties, and a deterministic CI invocation. Also land the `[tool.coverage.run]` and `[tool.bandit]` config skeletons in the same `pyproject.toml` (single-config-file convention) so Plan 04 (scorecard/coverage) and Plan 05 (CI bandit job) consume them.

Purpose: Implements TOOL-02 and ROADMAP SC#5 (mypy zero-error lenient baseline with `[[overrides]]`, no global strict). mypy is configured AFTER ruff because it is cleaner to drive to zero on already-import-sorted, formatted code (RESEARCH §Sequencing). No application behavior change.
Output: `pyproject.toml` extended with `[tool.mypy]` + overrides, `[tool.coverage.run]`, `[tool.bandit]`; a zero-error mypy baseline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-tooling-foundation-baseline-scorecard/01-CONTEXT.md
@.planning/phases/01-tooling-foundation-baseline-scorecard/01-RESEARCH.md
@.planning/phases/01-tooling-foundation-baseline-scorecard/01-PATTERNS.md
@pyproject.toml
@.planning/phases/01-tooling-foundation-baseline-scorecard/01-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan produces:
- `pyproject.toml` (EXTEND) — `[tool.mypy]` (lenient global: `python_version="3.12"`, `ignore_missing_imports=true`, `check_untyped_defs=false`, `disallow_untyped_defs=false`, `warn_unused_ignores=true`, `exclude=["^static/","^tests/","^scripts/"]`); one `[[tool.mypy.overrides]]` block listing the untyped third parties; `[tool.coverage.run]` (`source=["."]`, conditional `concurrency`); `[tool.bandit]` (`exclude_dirs=["tests","static","data","logs"]`, empty `skips` to be populated in Plan 05)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add [tool.mypy] lenient global + third-party overrides, drive to zero errors</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (append below the existing pytest + ruff tables — do not rewrite)
    - 01-PATTERNS.md §"pyproject.toml (EXTEND)" lines 167-179 (exact mypy config + overrides shape)
    - 01-RESEARCH.md lines 303-358 (lenient flags rationale, overrides for untyped 3rd-party, reaching-zero-day-1 strategy, deterministic --no-incremental invocation) and Open Question 2/3 (exclude tests)
    - 01-CONTEXT.md D-17
  </read_first>
  <action>
    Append `[tool.mypy]` below the ruff tables. Set the lenient global baseline (D-17): `python_version = "3.12"`, `ignore_missing_imports = true`, `check_untyped_defs = false`, `disallow_untyped_defs = false`, `disallow_incomplete_defs = false`, `warn_return_any = false`, `warn_unused_ignores = true`, `warn_redundant_casts = true`, `no_implicit_optional = false`, `exclude = ["^static/", "^tests/", "^scripts/"]` (tests are heavily stubbed — exclude day 1 per Open Q2). Do NOT set global `strict = true` (SC#5).
    Add one `[[tool.mypy.overrides]]` block with `module = ["mcp.*", "chromadb.*", "fastembed.*", "caldav.*", "icalendar.*", "croniter.*", "qrcode.*", "pyotp.*", "fitz.*", "markitdown.*", "duckduckgo_search.*", "faster_whisper.*", "youtube_transcript_api.*"]` and `ignore_missing_imports = true` (explicit/greppable even though the global flag covers them).
    Run `mypy --no-incremental --config-file pyproject.toml .` and drive residual errors to zero: for each genuine residual prefer a targeted `# type: ignore[error-code]  # reason` (never bare ignore); if a whole module is hopeless at baseline, add a narrow per-module override relaxing only the specific failing flag and log it as a ratchet tracking item in the SUMMARY. Keep the inverted-strictness list (modules to tighten later) EMPTY at baseline — those are added in Phase 3.
  </action>
  <verify>
    <automated>uv pip install --system --require-hashes -r requirements.lock >/dev/null 2>&amp;1 || pip install -r requirements.lock >/dev/null 2>&amp;1; uv pip install --system mypy==2.1.0 >/dev/null 2>&amp;1 || pip install mypy==2.1.0 >/dev/null 2>&amp;1; mypy --no-incremental --config-file pyproject.toml .; grep -q '\[\[tool.mypy.overrides\]\]' pyproject.toml &amp;&amp; ! grep -q 'strict = true' pyproject.toml</automated>
  </verify>
  <acceptance_criteria>
    `mypy --no-incremental --config-file pyproject.toml .` exits 0 (zero errors). `pyproject.toml` has `[tool.mypy]` with the lenient flags, at least one `[[tool.mypy.overrides]]` block for untyped third parties, and NO global `strict = true`. Any `# type: ignore` is code-specific with a reason; any baseline per-module relaxation is logged as a ratchet item.
  </acceptance_criteria>
  <done>mypy passes with zero errors under the lenient inverted-strictness config, deterministically (`--no-incremental`).</done>
</task>

<task type="auto">
  <name>Task 2: Add [tool.coverage.run] and [tool.bandit] config skeletons</name>
  <files>pyproject.toml</files>
  <read_first>
    - 01-PATTERNS.md §"pyproject.toml (EXTEND)" lines 181-188 ([tool.coverage.run] + [tool.bandit] shapes) and §".bandit / [tool.bandit]" lines 192-198
    - 01-RESEARCH.md lines 495-506 (bandit suppression conventions — populate skips AFTER the audit pass in Plan 05) and line 635 (coverage concurrency note)
    - 01-01-SUMMARY.md (whether Plan 01's coverage-stability run flagged async flakiness → whether to add concurrency)
  </read_first>
  <action>
    Append `[tool.coverage.run]` with `source = ["."]`. Add `concurrency = ["greenlet", "thread"]` ONLY if Plan 01's `pytest --cov=.` stability run (recorded in `01-01-SUMMARY.md`) reported async coverage flakiness — otherwise leave it commented with a note (Pitfall 11). Append `[tool.bandit]` with `exclude_dirs = ["tests", "static", "data", "logs"]` and an EMPTY `skips` (or commented placeholder) — the actual documented suppressions are populated in Plan 05 after the bandit audit pass (THREAT_MODEL.md treats shell/file/email subprocess as intentional admin features; B602–B607 will fire heavily and must be triaged, not blanket-disabled). Use the `[tool.bandit]` table in `pyproject.toml` rather than a separate `.bandit` file (single-config-file convention). Confirm the file still parses as valid TOML and the pytest/ruff/mypy blocks are intact.
  </action>
  <verify>
    <automated>python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); assert 'coverage' in d['tool'] and 'bandit' in d['tool'] and 'mypy' in d['tool'] and 'ruff' in d['tool'] and 'pytest' in d['tool']; print('TOML_OK')"</automated>
  </verify>
  <acceptance_criteria>
    `pyproject.toml` parses as valid TOML and contains `[tool.coverage.run]` (`source=["."]`) and `[tool.bandit]` (`exclude_dirs=[...]`, skips empty/placeholder). All five tool tables (pytest, ruff, mypy, coverage, bandit) coexist. The `concurrency` decision matches Plan 01's stability finding.
  </acceptance_criteria>
  <done>Coverage and bandit config skeletons exist in the single pyproject.toml, ready for Plan 04 and Plan 05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mypy config → CI mypy gate | Config governs the zero-error type-check gate CI hard-blocks on |
| bandit config → CI bandit gate | Suppressions (added Plan 05) govern which security findings block merge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | mypy cache nondeterminism causing CI flakiness | mitigate | `--no-incremental` in CI invocation; no committed `.mypy_cache` (RESEARCH line 354) |
| T-03-02 | Elevation of Privilege | over-broad `# type: ignore` masking real type errors | mitigate | `warn_unused_ignores = true`; require error-code-specific ignores with a reason (D-17) |
| T-03-03 | Tampering | empty bandit `skips` accidentally suppressing real findings later | accept | Skeleton ships empty; Plan 05's audit pass populates documented skips with rationale; V14 config-as-code |
| T-03-SC | Tampering | mypy version drift introducing new errors silently | mitigate | `mypy==2.1.0` pinned in `requirements-dev.lock` (Plan 01); CI installs that exact version (Plan 05) |
</threat_model>

<verification>
- `mypy --no-incremental --config-file pyproject.toml .` exits 0.
- `pyproject.toml` has `[tool.mypy]` lenient flags + `[[tool.mypy.overrides]]`; no global `strict = true`.
- `[tool.coverage.run]` and `[tool.bandit]` present; file parses as valid TOML.
- ruff and pytest blocks from earlier plans intact.
</verification>

<success_criteria>
- TOOL-02 / SC#5: mypy runs with zero errors using inverted-strictness config (lenient global + per-module `[[overrides]]`); no global strict. Enforceable in CI via `mypy --no-incremental`.
- Coverage + bandit config skeletons exist for downstream plans.
</success_criteria>

<output>
Create `.planning/phases/01-tooling-foundation-baseline-scorecard/01-03-SUMMARY.md` when done. Record: any `# type: ignore[code]` added, any baseline per-module relaxation (as a ratchet tracking item), the coverage `concurrency` decision.
</output>
