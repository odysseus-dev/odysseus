diff --git a/app.py b/app.py
index 0ff6e42..79a0328 100644
--- a/app.py
+++ b/app.py
@@ -1,5 +1,7 @@
 # app.py — slim orchestrator
 import os
+import sys
+from pathlib import Path
 
 # Windows: force HuggingFace/fastembed to COPY model files instead of symlinking.
 # On a network-share/UNC data dir Windows can't follow HF's symlinks ([WinError
@@ -10,6 +12,21 @@ if os.name == "nt":
     os.environ.setdefault("HF_HUB_DISABLE_SYMLINKS", "1")
     os.environ.setdefault("HF_HUB_DISABLE_SYMLINKS_WARNING", "1")
 
+
+def _configure_windows_frozen_data_paths() -> None:
+    """Use a writable data root for packaged Windows builds."""
+    if os.name != "nt" or not getattr(sys, "frozen", False):
+        return
+
+    base_dir = Path(
+        os.environ.get("LOCALAPPDATA") or (Path.home() / "AppData" / "Local")
+    )
+    data_dir = Path(os.environ.get("ODYSSEUS_DATA_DIR") or base_dir / "Odysseus" / "data")
+    data_dir.mkdir(parents=True, exist_ok=True)
+    os.environ.setdefault("ODYSSEUS_DATA_DIR", str(data_dir))
+    os.environ.setdefault("DATA_DIR", str(data_dir))
+    os.environ.setdefault("DATABASE_URL", f"sqlite:///{(data_dir / 'app.db').as_posix()}")
+
 from dotenv import load_dotenv
 # encoding="utf-8-sig" tolerates a UTF-8 BOM in .env — a common Windows gotcha
 # when the file is saved from Notepad. Without this, the first key parses as
@@ -17,6 +34,7 @@ from dotenv import load_dotenv
 # is silently ignored and the user is unexpectedly forced to log in (issue #142).
 # utf-8-sig reads plain UTF-8 (no BOM) identically, so this is safe everywhere.
 load_dotenv(encoding="utf-8-sig")
+_configure_windows_frozen_data_paths()
 import uuid
 
 import asyncio
@@ -323,7 +341,40 @@ else:
     logger.info("Auth middleware disabled (set AUTH_ENABLED=true to enable)")
 
 # ========= STATIC FILES =========
-os.makedirs(STATIC_DIR, exist_ok=True)
+def _resolve_windows_static_dir() -> str:
+    """Resolve bundled static assets for PyInstaller Windows builds.
+
+    Source runs still use the repository's ./static directory. Frozen Windows
+    executables need to look inside the extracted PyInstaller bundle instead of
+    relying on the current working directory.
+    """
+    if not getattr(sys, "frozen", False):
+        os.makedirs(STATIC_DIR, exist_ok=True)
+        return "static"
+
+    candidates = []
+    meipass = getattr(sys, "_MEIPASS", None)
+    if meipass:
+        candidates.append(Path(meipass) / "static")
+
+    exe_dir = Path(sys.executable).resolve().parent
+    candidates.append(exe_dir / "static")
+    candidates.append(exe_dir / "_internal" / "static")
+
+    for candidate in candidates:
+        if candidate.is_dir():
+            return str(candidate)
+
+    searched = "\n".join(str(path) for path in candidates)
+    raise RuntimeError(
+        "Static directory not found for Windows bundle. Searched:\n"
+        f"{searched}"
+    )
+
+
+STATIC_FILES_DIR = (
+    _resolve_windows_static_dir() if os.name == "nt" else "static"
+)
 
 
 class _RevalidatingStatic(StaticFiles):
@@ -342,7 +393,7 @@ class _RevalidatingStatic(StaticFiles):
         return resp
 
 
-app.mount("/static", _RevalidatingStatic(directory="static"), name="static")
+app.mount("/static", _RevalidatingStatic(directory=STATIC_FILES_DIR), name="static")
 
 # ========= GENERATED IMAGES =========
 @app.get("/api/generated-image/{filename}")
@@ -1006,3 +1057,13 @@ async def shutdown_event():
     except Exception as e:
         logger.warning(f"MCP shutdown error: {e}")
     logger.info("Application shutdown complete")
+
+if __name__ == "__main__" and os.name == "nt":
+    import uvicorn
+
+    uvicorn.run(
+        app,
+        host=os.getenv("ODYSSEUS_HOST", "127.0.0.1"),
+        port=int(os.getenv("ODYSSEUS_PORT", "7000")),
+        log_level=os.getenv("ODYSSEUS_LOG_LEVEL", "info"),
+    )
diff --git a/core/auth.py b/core/auth.py
index 4d35554..1f37c53 100644
--- a/core/auth.py
+++ b/core/auth.py
@@ -35,9 +35,12 @@ DEFAULT_PRIVILEGES = {
 # Admins get everything
 ADMIN_PRIVILEGES = {k: (True if isinstance(v, bool) else (0 if isinstance(v, int) else [])) for k, v in DEFAULT_PRIVILEGES.items()}
 
-DEFAULT_AUTH_PATH = os.path.join(
-    Path(__file__).parent.parent, "data", "auth.json"
+_DEFAULT_DATA_DIR = (
+    os.getenv("ODYSSEUS_DATA_DIR")
+    or os.getenv("DATA_DIR")
+    or os.path.join(Path(__file__).parent.parent, "data")
 )
+DEFAULT_AUTH_PATH = os.path.join(_DEFAULT_DATA_DIR, "auth.json")
 TOKEN_TTL = 60 * 60 * 24 * 7  # 7 days
 
 
diff --git a/core/constants.py b/core/constants.py
index 5dcf9e9..b3733d0 100644
--- a/core/constants.py
+++ b/core/constants.py
@@ -7,7 +7,7 @@ APP_VERSION = "0.9.1"
 # Base paths
 BASE_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__))) + "/"
 STATIC_DIR = os.path.join(BASE_DIR, "static")
-DATA_DIR = os.path.join(BASE_DIR, "data")
+DATA_DIR = os.getenv("ODYSSEUS_DATA_DIR") or os.getenv("DATA_DIR") or os.path.join(BASE_DIR, "data")
 
 # Data file paths
 SESSIONS_FILE = os.path.join(DATA_DIR, "sessions.json")
diff --git a/src/constants.py b/src/constants.py
index e44c6c4..fa662d1 100644
--- a/src/constants.py
+++ b/src/constants.py
@@ -7,7 +7,7 @@ APP_VERSION = "1.0.0"
 # Base paths
 BASE_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__))) + "/"
 STATIC_DIR = os.path.join(BASE_DIR, "static")
-DATA_DIR = os.path.join(BASE_DIR, "data")
+DATA_DIR = os.getenv("ODYSSEUS_DATA_DIR") or os.getenv("DATA_DIR") or os.path.join(BASE_DIR, "data")
 
 # Data file paths
 SESSIONS_FILE = os.path.join(DATA_DIR, "sessions.json")
